# ALIGNMENT_ui-home-and-profile

> 注：本文为 2026-01-14 阶段性对齐记录。当前产品行为已继续演进，默认筛选现为“全部”，锁屏背景改为透明 / 不透明两档，首次启动已升级为交互式新手教程。请以 `README.md`、`CHANGELOG.md` 与 `说明文档.md` 为准。

## 任务信息
- 任务名称：ui-home-and-profile
- 创建日期：2026-01-14
- 阶段：Align（需求对齐）

## 项目上下文分析
- 现有首页入口：`FocusLive/ContentView.swift`，使用 `NavigationView + ScrollView` 展示统计卡片与任务分组卡片。
- 数据层：SwiftData（`TaskGroup`、`TaskItem`），Live Activity 同步由 `ActivityManager` 驱动。
- 当前没有 Tab 结构或“我的”页面，所有内容集中在 `ContentView`。

## 原始需求
- IMG_4621.PNG 为当前主页视图。
- 主页视图需模仿 IMG_4620.PNG 的布局与风格。
- 需要新增“我的”页面，参考 IMG_4622.PNG。
  - 参考图路径：
    - `/Users/qingteng/Downloads/我的制作/FocusLive/参考图像/IMG_4620.PNG`
    - `/Users/qingteng/Downloads/我的制作/FocusLive/参考图像/IMG_4621.PNG`
    - `/Users/qingteng/Downloads/我的制作/FocusLive/参考图像/IMG_4622.PNG`

## 参考图观察要点（已读取）
### IMG_4620.PNG（目标主页）
- 顶部左侧为图标 + 标题“全部 1”，右侧为圆形“+”按钮。
- 顶部下方有三个胶囊筛选：全部、隐私空间、已完成（含图标）。
- 任务列表为大卡片样式，左侧圆形勾选，单行任务。
- 底部为胶囊型自定义 Tab：左“事项”选中，右“我的”。
- 背景为浅灰，卡片为白色，阴影柔和、圆角较大。

### IMG_4621.PNG（当前主页）
- 顶部标题为“FocusLive”，包含今日进度卡片与分组卡片列表。
- 顶部右侧为圆形“+”按钮。
- 主页信息密度高于 IMG_4620 目标样式。

### IMG_4622.PNG（目标“我的”页）
- 顶部欢迎语“你好 :)”，右侧“成为会员”按钮。
- 显示引导文字与“免费领取会员”卡片入口。
- 分区样式（通用 / 个性化 / 其他）+ 列表项卡片。
- 列表项包含图标、标题、副标题、右侧箭头或按钮（如“去开启”）。
- 底部胶囊 Tab，右侧“我的”选中。

## 需求理解（初版）
- 新增底部“事项/我的”切换入口，进入“我的”页面展示账号、会员、设置等列表项。
- 主页整体视觉向 IMG_4620.PNG 贴近，包括顶部标题样式、筛选胶囊、任务卡片样式与间距。
- “我的”页面为静态布局或半静态布局，暂不引入账号系统与网络功能。

## 已确认事项
- 主页信息结构保留“进度卡 + 分组卡列表”，仅做视觉改版。
- 底部切换入口可使用系统 TabView 并自定义外观。
- 多语言支持：中文 + 英文。
- 筛选胶囊需要真实功能：全部/隐私空间/已完成。
- 隐私空间事项不显示在实时活动里，并且需要通过 Face ID 验证才可以查看。
- 顶部“+”按钮行为为“新建分组”。
- “我的”页面列表项需要可点击行为与跳转。

## 范围与边界（初版）
- 范围内：主页 UI 调整、底部导航、我的页面 UI 新增、必要的视图拆分与代码重构。
- 范围外：数据结构大改、同步逻辑改写、会员体系/登录体系接入、远程接口。
  - 本次新增的隐私标记仅用于本地展示与 Live Activity 过滤，不涉及云端同步。

## 约束与规范
- 必须遵循项目现有技术栈（SwiftUI / SwiftData）。
- 新增函数必须包含函数级注释（功能、参数、返回值）。
- 需考虑多语言设计与 Liquid Glass 视觉风格的适配方式。

## 关键疑问（已更新状态）
1. 主页信息结构确认保留“进度卡 + 分组卡列表”（已确认）。
2. 筛选胶囊功能为真实功能（已确认）。
3. TabView 允许使用系统 TabView 并自定义外观（已确认）。
4. 顶部“+”按钮行为为新建分组（已确认）。
5. “我的”页列表项需要点击行为与跳转（已确认）。
6. 多语言支持中文 + 英文（已确认）。

## MCPsequential thinking（顺序化 ToDoList）
1. 读取参考图并提取视觉要点（已完成，2026-01-14）。
2. 输出 CONSENSUS 文档并锁定验收标准（已完成，2026-01-14）。
3. 设计 UI 结构（首页/我的页/底部导航）与组件拆分方案。
4. 实现视图结构与样式，保持现有数据与同步逻辑不变。
5. 自检与验收记录更新（手动检查 + 预览验证）。

## 当前结论
- 需求确认完成，可进入 CONSENSUS 阶段锁定验收标准。
