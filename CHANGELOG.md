# 更新日志

## 2026-04-03

### 首页与引导

- 移除“添加示例数据”流程，空状态改为直接创建第一个分组
- 新增交互式新手教程
- 新手教程支持直接设置锁屏字体大小、已完成事项显示、每日鼓励开关、卡片背景模式
- 新手教程新增灵动岛提示：左滑可临时关闭显示
- 首页默认筛选改为 `全部`
- 首页筛选顺序调整为 `全部 / 未完成 / 已完成 / 隐私空间`

### 添加分组

- 顶部 `+` 按钮改为锚定式下拉菜单
- 空状态下的“创建第一个分组”入口同步改为下拉菜单
- 支持在菜单中直接选择：
  - 传统待办事项
  - 每日打卡
  - 提醒事项

### 每日鼓励

- 首页 `全部` 页底部新增每日鼓励卡片
- 每日鼓励支持手动刷新
- 每日鼓励支持自定义文案和作者
- 每日鼓励开关改为独立生效，不再依赖“无分组显示”条件
- 每日鼓励可同步到锁屏鼓励卡片

### 锁屏卡片与灵动岛

- 默认字体大小改为 `150%`
- 分组标题字号与任务内容字号同步
- 支持透明 / 不透明两种背景模式
- 支持显示已完成事项开关
- 已完成事项在锁屏上改为更稳定的横线标记
- 修复锁屏勾选任务后字体颜色回退到默认值的问题
- 修复灵动岛展开区把 emoji 当成 SF Symbol 的问题

### 文档

- 更新 `README.md`
- 更新 `QUICKSTART.md`
- 更新 `SETUP_CHECKLIST.md`
- 更新 `说明文档.md`

### 构建验证

- 已执行：

```bash
xcodebuild -scheme FocusLive -project FocusLive.xcodeproj -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO -quiet build
```

- 结果：通过
